import styled from 'styled-components'
import {
  justifyContent,
  justifySelf,
  display,
  flexWrap,
  flexBasis,
  flexDirection,
  flex
} from 'styled-system'

const Flex = styled.div`
  ${justifyContent}
  ${justifySelf}
  ${display}
  ${flexWrap}
  ${flexBasis}
  ${flexDirection}
  ${flex}
`

export default Flex
