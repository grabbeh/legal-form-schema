import styled from 'styled-components'
import {
  color,
  space,
  fontSize,
  fontWeight,
  letterSpacing,
  lineHeight,
  textAlign,
  fontFamily
} from 'styled-system'

const Text = styled.div`
  ${color}
  ${space}
  ${fontSize}
  ${fontWeight}
  ${letterSpacing}
  ${lineHeight}
  ${textAlign}
  ${fontFamily}
`

export default Text
