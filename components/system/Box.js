import styled from 'styled-components'
import { space, width, color, height, backgroundImage } from 'styled-system'

const Box = styled.div`
  ${space}
  ${width}
  ${color}
  ${height}
  ${backgroundImage}
`

export default Box
